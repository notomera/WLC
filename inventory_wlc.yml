base:
    #default data
    #for device types and the corresponding argument to be used visit the link below
    #https://github.com/ktbyers/netmiko/blob/master/netmiko/ssh_dispatcher.py#L70
    device_type: cisco_wlc
    username: admin
    password: Admin123
    #the separator between commands is ','
    commands: show ap join stats summary all
devices:
    #add data here to overwrite the default
    device_1:
        host: 10.100.0.100

